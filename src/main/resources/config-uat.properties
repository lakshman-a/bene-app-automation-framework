# ===================================================
#  bene-app Automation Framework - UAT Environment
#  Selected with -Denv=uat
#  NOTE: point these at your real UAT hosts when available.
# ===================================================

# ---- Application under test ----
app.base.url=http://bene-ui-uat.local
api.base.url=http://bene-api-uat.local
api.token=asdfbdnjasdjasnjd7712379719y997
# API endpoint paths (kept in config, not in feature files)
api.accounts.path=/api/accounts
api.beneficiaries.path=/api/beneficiaries

# ---- Test users (end-user portal) ----
test.user.username=johndoe
test.user.password=demo123
# ---- Admin portal ----
admin.username=admin
admin.password=admin123

# ---- Browser / Playwright ----
browser.type=chromium
browser.headless=true
browser.slow.mo=0
browser.timeout=30000
viewport.width=1920
viewport.height=1080

# ---- Database (bene_schema on PostgreSQL) ----
db.url=jdbc:postgresql://bene-db-uat.local:5432/postgres
db.user=postgres
db.password=changeme
db.schema=bene_schema

# ---- Reporting ----
allure.results.directory=target/allure-results

# Screenshots: true = attach after every step, false = only on failure
screenshot.on.each.step=false
